# Copy to .env for local development.

# Where the ASPIRE FastAPI backend is listening.
# The browser calls this directly, so it must be reachable from the browser --
# not a container-internal hostname. The backend enables CORS for local dev.
VITE_ASPIRE_API_URL=http://localhost:8000
